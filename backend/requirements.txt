fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.9
psycopg[binary]>=3.2

# CPU-only torch build for docling below - PyPI's default `torch` wheel drags
# in ~1.5GB+ of nvidia-*/CUDA packages (cublas, cudnn, triton, ...) that this
# VPS deploy has no GPU to use. Degrades gracefully if this index doesn't
# have a matching wheel (falls back to PyPI), so this is a pure size
# reduction, not a hard requirement.
--extra-index-url https://download.pytorch.org/whl/cpu

# document pipeline: OCR routing (docs/05-AI-EXTRACTION-STRATEGY.md)
pymupdf>=1.24
docling>=2.0

# document pipeline: real-HTTP Fetcher for the downloader/ingest CLI
# (app/pipeline/run_ingest.py) - also used by tests and FastAPI's TestClient
httpx>=0.27

# dev/test
pytest>=8.3

# auth & accounts (docs/10-AUTH-AND-ACCOUNTS.md, docs/13-COMPETITIVE-STRATEGY.md §5):
# argon2id password hashing, JWT access tokens, email validation
argon2-cffi>=25.1
PyJWT>=2.9
email-validator>=2.2
