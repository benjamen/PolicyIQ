[pytest]
pythonpath = .
testpaths = tests
markers =
    playwright: requires real Chromium + real network access, skipped by default (see backend/tests/ocr/test_docling_backend.py for the same pattern)
